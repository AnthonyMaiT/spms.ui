import { EventTimeValidator } from './event-time-validator';

describe('EventTimeValidator', () => {
  it('should create an instance', () => {
    expect(new EventTimeValidator()).toBeTruthy();
  });
});
